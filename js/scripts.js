$(document).ready(function() {
  $("form#language").submit(function(event) {
    event.preventDefault();
    var xp = ($("input#xp").val());
    var uiorux = ($("input#uiorux").val());
    var os = ($("input#os").val());
    var drink = ($("input#drink").val());
    var music = ($("input#music").val());
    console.log(userInput)
}